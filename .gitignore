*.code-workspace
.cursor/rules/dev-environment.mdc
docs/chats/*
docs/SESSION-SUMMARY-*.md
/target
proptest-regressions/

# Gas Town Infrastructure (Moved to ~/gastown/stroma/)
# Track: Only .beads/*.bead files (architectural docs) and issues.jsonl
# Ignore: All Gas Town instance infrastructure (operational machinery)

# =============================================================================
# Gas Town infrastructure directories (now in ~/gastown/stroma/)
# =============================================================================
mayor/
polecats/
refinery/
witness/
crew/
daemon/
deacon/
stroma/

# =============================================================================
# Gas Town runtime files (now in ~/gastown/stroma/)
# =============================================================================
.events.jsonl
.feed.jsonl
settings/
plugins/
.claude/

# =============================================================================
# Runtime state files (transient)
# =============================================================================
**/state.json
**/*.lock
**/registry.json

# =============================================================================
# Runtime state directories (gitignored ephemeral data)
# =============================================================================
**/.runtime/

# =============================================================================
# Rig .beads symlinks (point to ignored mayor/rig/.beads, recreated on setup)
# =============================================================================
# Add rig-specific symlinks here, e.g.:
# gastown/.beads

# =============================================================================
# OS and editor files
# =============================================================================
.DS_Store
*~
*.swp
*.swo
.vscode/
.idea/

# =============================================================================
# Explicitly track (override above patterns)
# =============================================================================
# Note: .beads/ has its own .gitignore that handles SQLite files
# and keeps issues.jsonl, metadata.json, config file as source of truth

# Gas Town (added by gt)
.runtime/
.claude/
# Note: .beads/ has its own .gitignore for runtime files
# We track *.bead files and issues.jsonl for team collaboration
.logs/

# Beads export state (updates on every git operation)
.beads/export-state/

# Gas Town (added by gt)
.beads/
